import { FOOTER_LOGO } from "@/data/footer";
import type { FooterBannerData } from "@/types/footer-banner";

/** Bandeau sous footer — logo v1 coloré (visible sur fond sombre). */
export const FOOTER_BANNER: FooterBannerData = {
  alt: "Back2Mboa",
  wordmark: FOOTER_LOGO,
};
